# UNIVERSIDAD CONTINENTAL
## Taller de Proyectos 2

# 12. Arquitectura de Software – Modelo C4

**Nombre del Proyecto:** EcoLogística Lima – Optimizador de Rutas Sostenibles para DistriRápido S.A.C.  
**Fecha:** 04/09/2026  
**Versión:** 1.0.0  
**Project Manager:** Carhuapoma Fano, Eilene Elizabeth  

---

## 1. Introducción

Se presenta la arquitectura del sistema EcoLogística Lima utilizando el modelo **C4** (Context, Containers, Components, Code) en sus tres primeros niveles, tal como exige la consigna de arquitectura.  
Los diagramas se generan en sintaxis **Mermaid** para garantizar trazabilidad y versionamiento en el repositorio.

---

## 2. Nivel 1 – Diagrama de Contexto (System Context)

Muestra el sistema como una caja negra y sus interacciones con los actores y sistemas externos.

```mermaid
C4Context
    title Diagrama de Contexto - EcoLogística Lima

    Person(gerente, "Gerente de Operaciones", "Toma decisiones estratégicas y consulta indicadores de sostenibilidad")
    Person(operador, "Operador de Logística", "Gestiona pedidos, flota y solicita optimizaciones")
    Person(conductor, "Conductor de Reparto", "Consulta rutas asignadas en Modo Conductor")
    Person(cliente, "Cliente / Bodeguero", "Registra preferencias de entrega")
    Person(admin, "Administrador del Sistema", "Configura parámetros y gestiona usuarios")
    Person(docente, "Docente / Auditor", "Revisa entregables y documentación")

    System(eco, "EcoLogística Lima", "Plataforma web de optimización de rutas sostenibles (VRPTW + Green VRP)")

    System_Ext(waze, "Waze CCP / Google Maps", "Datos de tráfico e incidentes en tiempo real")
    System_Ext(osm, "OpenStreetMap", "Mapa base y geocodificación")
    System_Ext(email, "Servicio de Notificaciones", "Alertas y reportes por correo/SMS")

    Rel(gerente, eco, "Consulta dashboard y reportes", "HTTPS")
    Rel(operador, eco, "Gestiona pedidos, flota y genera rutas", "HTTPS")
    Rel(conductor, eco, "Consulta ruta y confirma entregas", "HTTPS")
    Rel(cliente, eco, "Registra preferencias", "HTTPS")
    Rel(admin, eco, "Administra usuarios y parámetros", "HTTPS")
    Rel(docente, eco, "Revisa documentación y métricas", "HTTPS")

    Rel(eco, waze, "Consulta tráfico e incidentes", "API REST")
    Rel(eco, osm, "Obtiene mapa base y tiles", "HTTPS")
    Rel(eco, email, "Envía notificaciones y reportes", "SMTP / API")
```
---
## 3. Nivel 2 – Diagrama de Contenedores (Containers)
Descompone el sistema en contenedores desplegables (aplicaciones, bases de datos, servicios).

```mermaid
C4Container
    title Diagrama de Contenedores - EcoLogística Lima

    Person(operador, "Operador / Gerente / Conductor / Admin", "Usuarios del sistema")

    System_Boundary(eco, "EcoLogística Lima") {
        Container(spa, "Single Page Application", "React + TypeScript + Vite", "Interfaz de usuario (Dashboard, Modo Conductor, Mapas, Formularios)")
        Container(api, "API Backend", "Python + FastAPI", "Lógica de negocio, autenticación, orquestación de optimización y reportes")
        Container(optimizer, "Motor de Optimización", "Python (DEAP / OR-Tools)", "Ejecuta metaheurísticas VRPTW + Green VRP")
        ContainerDb(db, "Base de Datos", "PostgreSQL + PostGIS", "Almacena flota, pedidos, rutas, usuarios, zonas de restricción y auditoría")
        Container(cache, "Caché / Colas", "Redis (opcional MVP)", "Sesiones, resultados de optimización y tareas asíncronas")
    }

    System_Ext(waze, "Waze CCP / Google Maps", "Tráfico e incidentes")
    System_Ext(osm, "OpenStreetMap / Leaflet", "Mapa base")
    System_Ext(storage, "Almacenamiento de Archivos", "Sistema de archivos / S3 compatible", "Reportes PDF")

    Rel(operador, spa, "Usa", "HTTPS")
    Rel(spa, api, "Consume", "REST / JSON + JWT")
    Rel(api, optimizer, "Invoca", "Llamada interna / cola")
    Rel(api, db, "Lee y escribe", "SQL")
    Rel(api, cache, "Lee y escribe", "Redis Protocol")
    Rel(optimizer, db, "Lee pedidos y flota / escribe rutas", "SQL")
    Rel(api, waze, "Consulta tráfico", "API REST")
    Rel(spa, osm, "Renderiza mapa", "HTTPS tiles")
    Rel(api, storage, "Guarda reportes PDF", "API / filesystem")
```
#### Descripción del Nivel 2

| Componente | Tecnología | Responsabilidad principal |
|------------|-----------|--------------------------|
| SPA | React + TypeScript + Vite | Toda la experiencia de usuario |
| API Backend | FastAPI | Autenticación, CRUD, orquestación, generación de reportes |
| Motor de Optimización | Python + DEAP / OR-Tools | Cálculo de rutas (proceso intensivo en CPU) |
| Base de Datos | PostgreSQL + PostGIS | Persistencia transaccional y geoespacial |
| Caché / Colas | Redis (opcional en MVP) | Sesiones y desacople del motor de optimización |

---

## 4. Nivel 3 – Diagrama de Componentes (API Backend)
```mermaid
C4Component
    title Diagrama de Componentes - API Backend (FastAPI)

    Container_Boundary(api, "API Backend - FastAPI") {
        Component(auth, "Auth Controller + Service", "FastAPI / JWT", "Login, refresh token, control de intentos fallidos")
        Component(users, "Users Controller + Service", "FastAPI", "CRUD de usuarios y roles")
        Component(fleet, "Fleet Controller + Service", "FastAPI", "Gestión de vehículos")
        Component(drivers, "Drivers Controller + Service", "FastAPI", "Gestión de conductores y jornadas")
        Component(orders, "Orders Controller + Service", "FastAPI", "Gestión de pedidos y preferencias")
        Component(routing, "Routing Controller + Service", "FastAPI", "Solicitud de optimización y re-optimización")
        Component(optimizer_client, "Optimizer Client", "Python", "Interfaz con el Motor de Optimización")
        Component(maps, "Maps & Traffic Service", "FastAPI", "Integración con Waze/OSM y zonas de restricción")
        Component(dashboard, "Dashboard & Reports Service", "FastAPI", "Indicadores, emisiones y generación de PDF")
        Component(audit, "Audit Service", "FastAPI", "Registro de eventos críticos")
        Component(repo, "Repositories", "SQLAlchemy", "Acceso a datos (patrón Repository)")
    }

    ContainerDb(db, "PostgreSQL + PostGIS", "Base de datos")
    Container(optimizer, "Motor de Optimización", "DEAP / OR-Tools")
    System_Ext(waze, "Waze CCP")

    Rel(auth, repo, "Usa")
    Rel(users, repo, "Usa")
    Rel(fleet, repo, "Usa")
    Rel(drivers, repo, "Usa")
    Rel(orders, repo, "Usa")
    Rel(routing, optimizer_client, "Invoca")
    Rel(routing, repo, "Usa")
    Rel(optimizer_client, optimizer, "Ejecuta")
    Rel(maps, waze, "Consulta")
    Rel(maps, repo, "Usa")
    Rel(dashboard, repo, "Usa")
    Rel(audit, repo, "Usa")
    Rel(repo, db, "SQL")
```

#### Descripción de Componentes principales

| Componente | Responsabilidad |
|------------|----------------|
| Auth Controller + Service | Autenticación JWT, bloqueo por intentos fallidos (RN-019) |
| Fleet / Drivers / Orders | CRUD completo de las entidades de negocio |
| Routing Controller | Orquesta la generación y re-optimización de rutas |
| Optimizer Client | Desacopla la API del motor de cálculo intensivo |
| Maps & Traffic Service | Integra datos de tráfico y valida zonas restringidas |
| Dashboard & Reports Service | Calcula indicadores y genera PDFs de sostenibilidad |
| Audit Service | Cumple RNF-010 y Ley N° 29733 |
| Repositories | Única capa de acceso a datos (SQLAlchemy) |

## 5. Decisiones Arquitectónicas Relevantes
1. Separación del Motor de Optimización
Se ejecuta como proceso independiente (o worker) para no bloquear la API y cumplir los SLAs de ≤ 45 s / ≤ 30 s.
2. API Gateway implícito
FastAPI actúa como único punto de entrada. Toda autenticación y autorización se resuelve en la capa de Auth.
3. Base de datos única
PostgreSQL + PostGIS cubre tanto datos transaccionales como geoespaciales, evitando la complejidad de un polyglot persistence en el MVP.
4. Frontend desacoplado
La SPA consume únicamente la API REST documentada (OpenAPI). Permite evolución independiente y facilita el “Modo Conductor” liviano.
5. Principio de mínimo privilegio
Cada componente solo accede a los repositorios que necesita. El control de acceso fino se implementa en los servicios según la matriz RBAC del documento 08.
6. Observabilidad básica
Health checks, logs estructurados y tabla de auditoría permiten cumplir RNF-006 y RNF-010 desde la Iteración 2.

## 6. Vista de Despliegue (resumen)
````mermaid
flowchart TB
    subgraph Cloud["Cloud Provider (Railway / Render / AWS)"]
        SPA["SPA React<br/>Estáticos + CDN"]
        API["API FastAPI<br/>Contenedor Docker"]
        OPT["Motor Optimización<br/>Worker"]
        DB["PostgreSQL + PostGIS"]
        REDIS["Redis (opcional)"]
    end

    Usuario["Usuarios"] -->|HTTPS| SPA
    SPA -->|REST/JWT| API
    API --> OPT
    API --> DB
    API --> REDIS
    OPT --> DB
````
